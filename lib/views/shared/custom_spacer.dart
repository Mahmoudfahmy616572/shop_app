import 'package:flutter/material.dart';

class CustomSapcer extends StatelessWidget {
  const CustomSapcer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 25,
    );
  }
}
